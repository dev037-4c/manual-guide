<!-- CSS-Only 이미지 슬라이더 (Obsidian 호환) -->
<div class="snap-slider" id="gallery">
  <div class="snap-viewport">
    <div class="snap-track">
      <section class="snap-slide" id="s1">
        <img src="https://picsum.photos/1200/675?random=201" alt="슬라이드1">
      </section>
      <section class="snap-slide" id="s2">
        <img src="https://picsum.photos/1200/675?random=202" alt="슬라이드2">
      </section>
      <section class="snap-slide" id="s3">
        <img src="https://picsum.photos/1200/675?random=203" alt="슬라이드3">
      </section>
      <section class="snap-slide" id="s4">
        <img src="https://picsum.photos/1200/675?random=204" alt="슬라이드4">
      </section>
    </div>
  </div>

  <!-- 네비게이션 (앵커) -->
  <a class="snap-nav prev" href="#s4" aria-label="이전">‹</a>
  <a class="snap-nav next" href="#s2" aria-label="다음">›</a>

  <!-- 도트 -->
  <div class="snap-dots">
    <a href="#s1" aria-label="1번"></a>
    <a href="#s2" aria-label="2번"></a>
    <a href="#s3" aria-label="3번"></a>
    <a href="#s4" aria-label="4번"></a>
  </div>
</div>

<style>
/* 컨테이너 */
.snap-slider {
  --radius: 16px;
  --shadow: 0 6px 20px rgba(0,0,0,.12);
  position: relative;
  width: 100%;
  max-width: 720px;
  margin: 0 auto;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, "Noto Sans KR", sans-serif;
}

/* 뷰포트: 한 장만 보이도록 */
.snap-viewport {
  overflow: hidden;
  border-radius: var(--radius);
}

/* 트랙: 가로 스크롤 + 스냅 */
.snap-track {
  display: flex;
  scroll-snap-type: x mandatory;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  scroll-behavior: smooth;
  scrollbar-width: none;              /* Firefox */
}
.snap-track::-webkit-scrollbar { display: none; }  /* WebKit */

/* 슬라이드: 항상 100% 너비 → 1장만 표시 */
.snap-slide {
  position: relative;
  flex: 0 0 100%;
  scroll-snap-align: center;
}
.snap-slide img {
  display: block;
  width: 100%;
  height: auto;
  border-radius: var(--radius);
  box-shadow: var(--shadow);
}

/* 화살표 네비게이션 (앵커) */
.snap-nav {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 36px; height: 36px;
  border-radius: 999px;
  background: rgba(255,255,255,.9);
  box-shadow: 0 2px 10px rgba(0,0,0,.16);
  font-size: 22px;
  line-height: 36px;
  text-align: center;
  text-decoration: none;
  color: inherit;
  z-index: 2;
}
.snap-nav.prev { left: 8px; }
.snap-nav.next { right: 8px; }

/* 도트 */
.snap-dots {
  display: flex;
  justify-content: center;
  gap: 8px;
  margin-top: 10px;
}
.snap-dots a {
  width: 8px; height: 8px;
  border-radius: 999px;
  background: #d1d5db;
  display: inline-block;
  text-decoration: none;
}

/* 다크 테마 */
:root[data-theme="dark"] .snap-nav { background: rgba(25,27,33,.9); }
:root[data-theme="dark"] .snap-dots a { background: #2d3340; }
</style>
